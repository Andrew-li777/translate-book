# 架构与流程图

## 一、总览

```
┌─────────────────────────────────────────────────────────────────────┐
│                         Cloudflare（HTTPS）                          │
│  trs.andrew-li.top ── Origin Rule: Hostname → 8080                  │
└────────────────────────────────┬────────────────────────────────────┘
                                 ▼
                      ┌─────────────────────┐
                      │   Caddy 2.6.2       │  只做反代（不做认证）
                      │   /etc/caddy/       │  ──────────────────────
                      │   按 Host 分流       │  trs.*        → 8788
                      │                     │  :8080 兜底   → 8787(scan)
                      └─────────┬───────────┘
                                ▼
        ┌───────────────────────────────────────────────────────────┐
        │                translate-web（FastAPI :8788）              │
        │  systemd translate-web.service（自启+自愈）                  │
        │                                                            │
        │  web/main.py ── API 层（见下）                              │
        │  web/auth.py  AuthMiddleware：游客只读 / 管理员全功能          │
        │  web/guest_rules.py 游客白名单（读端点/扩展名）                 │
        │  web/books.py 书库扫描/状态判定                              │
        │  web/jobs.py  文件系统任务队列                               │
        │  web/consumer.py 队列机械操作（pick/finish/merge）           │
        │  web/static/  单页前端（index.html + app.js）                │
        │                                                            │
        │  ┌──────────────── merge watcher（60s 线程）───┐            │
        │  │                                             ▼            │
        └──┼──────────────────────────────────────────────────────────┘
           ▼
   ┌──────────────────────┐      ┌──────────────────────────────────────┐
   │  翻译工作区           │◄────►│  translate-direct.service（D 方案主引擎）│
   │  /root/translate/work│       │  systemd 守护，ThreadPool(3) 直连 amd API│
   │  {书名}_temp/        │       │  reasoning_effort=none, fix_images 兜底    │
   │  jobs/{id}/          │       │  ~110 块/h, 10.8M 常驻                    │
   └──────────┬───────────┘       └──────────────────────────────────────┘
              ▼                    ┌──────────────────────────────────────┐
   ┌──────────────────────┐        │  Hermes cron ab9ef9653179（兜底）        │
   │  book/scripts 引擎    │        │  每 3 分钟 agent 模式, delegate_task 3 块 │
   │  （.venv + Calibre    │        │  ▼ 互斥守卫：D active 时 cron 待命；      │
   │   + Pandoc）          │        │  ▼ D 失效时 cron 自动接管翻译            │
   │  convert→翻译→merge   │        └──────────────────────────────────────┘
   │  →5 格式成品           │
   └──────────────────────┘
      ▲
      └── merge watcher（60s 线程，translate-web 内）在书译完后自动构建成品
```

## 二、成品分发（E 方案 · R2）

```
浏览器 ──点下载──► GET /api/download/{name}/book.pdf      （管理员凭据；游客被 403）
                      │
                      ├─ R2 已配置 → 返回预签名 URL
                      │     └─► window.location.href 顶级导航 ──► R2 桶 books/{书名}/book.pdf
                      │           （15 分钟时效，attachment 保原名，浏览器直写磁盘）
                      └─ R2 不可用 → {url:null, fallback:"/api/books/.../download/..."}
                            └─► 前端回退 fetch+Blob → FileResponse 本地成品

本地 work/{书名}_temp/book.{pdf,docx,epub}  ══ r2_sync.py 每 15 分钟幂等上传 ══► R2
   ▲ 本地必须保留（_status()/_file_summary() 依赖）——R2 是分发通道，不是备份
```

## 三、数据流（一次完整翻译）

```
上传(PDF/DOCX/EPUB ≤100MB)
  → POST /api/upload → job {pending}
  → convert 后台线程 → {converting}
  → 生成 chunk0001..N + manifest → {ready}
  → translate-direct.service（D 方案主引擎）持续轮询：
      consumer.py pick → 3 块
      直连通道 API 翻译（PROVIDERS: amd → ark；reasoning_effort=none + 术语表 + 邻居上下文 + fix_images 补图）
      ⚠️ 两跳 2026-09-19 起均不可用（amd 对话模型下线 / ark 周配额耗尽），见问题记录 #29
      output_chunk{i}.md 落盘
      consumer.py finish → record + 进度
  → 全书完成 → consumer.py request-merge → {merging}
  → Web merge watcher（60s）检测 → merge_and_build.py
  → 5 格式成品（pdf/docx/epub/html/output.md）→ {done}
  （D 失效时 cron ab9ef9653179 每 3 分钟接管，delegate_task 3 块，逻辑同上）
```

## 三、任务状态机

```
pending ──convert──► converting ──convert成功──► ready
                       │                            │
                       └──失败──► failed             │ cancel ─► cancelled
                                                    ▼
                                              translating（D 主引擎 / cron 兜底译块）
                                                    │ 全部完成
                                                    ▼
                                              merging（watcher）
                                                    │ 成功/失败
                                                    ▼
                                              done / failed
```

任意状态可被 trash（进行中 409 保护）→ 回收站 → restore。

## 四、API 清单

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/` | 前端首页 |
| GET | `/static/*` | 静态资源 |
| GET | `/api/books` | 书库列表 |
| GET | `/api/books/{name}` | 单书详情 |
| GET | `/api/books/{name}/chunk/{cid}` | 章节内容（原文/译文） |
| GET | `/api/books/{name}/file/{path:path}` | 工作区任意文件 |
| GET | `/api/books/{name}/render/{path:path}` | markdown 渲染 |
| GET | `/api/books/{name}/download/{path:path}` | 下载（5 格式成品，本地直连 / 回退路径） |
| GET | `/api/download/{name}/{path:path}` | **成品分发（E 方案）**：返回 R2 预签名 URL，失败时返 local 回退 |
| GET | `/api/books/{name}/cover` | 封面 |
| GET/PUT | `/api/books/{name}/glossary` | 术语表读写（保存即重译） |
| POST | `/api/upload` | 上传新书（multipart） |
| GET | `/api/jobs` | 任务列表 |
| GET | `/api/jobs/{id}` | 任务详情 |
| POST | `/api/jobs/{id}/cancel` | 取消（仅 ready） |
| GET | `/api/jobs/{id}/stream` | SSE 实时进度 |
| GET | `/api/search?q=&scope=` | 全文搜索（trans/src/all） |
| GET | `/api/trash` | 回收站列表 |
| POST | `/api/books/{name}/trash` · `/api/trash/books/{name}/restore` · `/api/trash/books/empty` | 书回收/恢复/清空 |
| POST | `/api/jobs/{id}/trash` · `/api/trash/jobs/{id}/restore` · `/api/trash/jobs/empty` | 任务回收/恢复/清空 |

### 权限分层（游客 / 管理员）

鉴权由 `web/auth.py` 的 `AuthMiddleware` 统一判定，白名单在 `web/guest_rules.py`。**Caddy 不做认证**（否则游客会被 401 挡在门外）。

| 类别 | 端点 | 游客 | 管理员 |
|---|---|---|---|
| 首页/静态 | `/`、`/favicon.ico`、`/static/*` | ✅ | ✅ |
| 书库读 | `/api/books`、`/api/books/{name}`、`/chunk/{cid}`、`/render/{path}`、`/cover`、`/glossary`(GET) | ✅ | ✅ |
| 文件读 | `/api/books/{name}/file/{path}` | ⚠️ **扩展名白名单**（md/txt/srt/csv/图片/html），json/yml 拒绝 | ✅ |
| 搜索 | `/api/search` | ✅（10 次/分/IP 限频）| ✅ |
| 任务列表 | `/api/jobs` | ✅（仅列表）| ✅ |
| 任务详情/SSE | `/api/jobs/{id}`、`/api/jobs/{id}/stream` | ❌ 403 | ✅ |
| 回收站 | `/api/trash*` | ❌ 403 | ✅ |
| **下载成品** | `/api/books/{name}/download/{path}` | ❌ 403 | ✅ |
| **成品链接** | `/api/download/{name}/{path}` | ❌ 403（前缀不在白名单，天然拒绝）| ✅ |
| 写操作 | `/api/upload`、trash/restore/empty、glossary PUT、jobs cancel | ❌ 403 | ✅ |
| 凭据无效 | 任意 | — | **401（不降级为游客）** |

> 游客访问受限资源返回 **403 + `X-Guest-Only: 1`**；凭据错误返回 **401**（防「伪装成游客」绕过白名单）。

## 五、目录结构

```
/root/translate/
├── book/                    # translate-book skill 引擎（上游仓库移植）
│   ├── SKILL.md             # skill 蓝图（含 P3a/P3b/P3c/P4 已实现标注）
│   ├── scripts/             # 11 个流水线脚本（convert/run_state/glossary/merge...）
│   ├── tests/               # 上游单元测试
│   ├── assets/              # 海报资产
│   └── .venv/               # Python 虚拟环境
├── web/                     # translate-web 在线网站
│   ├── main.py              # FastAPI 应用（684 行）
│   ├── auth.py              # AuthMiddleware（游客/管理员鉴权，bcrypt）
│   ├── guest_rules.py       # 游客只读白名单判定
│   ├── books.py             # 书库逻辑（含 _progress() 进度解析）
│   ├── jobs.py              # 任务队列
│   ├── consumer.py          # cron 消费者辅助（pick/finish/request-merge/status）
│   ├── translate_direct.py  # D 方案主引擎（直连 API，ThreadPool 3）
│   ├── fix_images.py        # 图片引用后处理（补缺失/删多余）
│   ├── r2.py                # E 方案：R2 上传/预签名/删除封装（失败静默降级）
│   ├── r2_sync.py           # 成品幂等同步脚本（--dry-run 支持）
│   ├── translate-r2sync.service/.timer  # 每 15 分钟增量同步
│   ├── config.py            # 路径/常量配置
│   ├── static/              # index.html + app.js
│   └── translate-web.service # systemd 单元
├── work/                    # 翻译工作区（运行数据，gitignore）
│   ├── {书名}_temp/         # 每本书：chunk*/output_chunk*/run_state/glossary/成品
│   ├── jobs/{id}/           # 任务元数据 + progress.json + merge_request.json
│   └── .trash/              # 回收站
└── 迭代文档/                 # 本项目文档
```

## 六、关键机制

### 断点续译
output_chunk{i}.md 落盘即「已翻译」事实 → 中断后 pick 自动 record_only → 绝不重译。

### 术语表重译
PUT glossary → 内容哈希变化 → affected_chunks 重置 ready → cron 下轮接手。

### merge 防坑
- watcher 不用 `--cleanup`（对照 tab 依赖 chunk 文件）
- source_empty 补丁（空尾 chunk 合法）

### 鉴权分层（游客 / 管理员）
- `AuthMiddleware` 是所有请求的第一道关：无 `Authorization` = 游客（只读白名单），凭据有效 = 管理员，**凭据无效 = 401 不降级**
- 白名单集中在 `guest_rules.py`（读端点 + file 扩展名），改权限不用动反代配置
- **Caddy 只转发不做认证**——反代层做认证会把游客一起挡掉，且前端拿不到可编程响应
- 前端凭据存 `localStorage`，**必须显式 `fetch` 带 `Authorization` 头**；下载/导出走 `fetch + Blob`（`window.location`/`<a href>` 不带凭据）

### 进度解析
`books.py:_progress()` 把状态串解析为 `{done,total,pct,active}`；前端 `progOf()` 保留同款兜底解析，**前后端部署顺序不构成硬依赖**（详见经验总结 #17）。

### 成品分发（R2）
- 成品（pdf/docx/epub）走 R2 预签名 URL，前端 `window.location.href` 顶级导航直下——**不占 VPS 出网流量、不进浏览器内存**
- 端点 `/api/download/{name}/{path}`：R2 不可用/未配置 → 返回 `fallback` 指回本地 `/api/books/{name}/download/{path}`，前端自动回退
- `r2_sync.py` 每 15 分钟扫描 `work/*_temp` 幂等同步（同尺寸跳过），零侵入不改 merge 流程
- 回收站**永久删除**（`/api/trash/books/empty`）才删 R2 对象；移入回收站不删（要能恢复）
- **本地成品永不删**：`_status()` 判 `done`、`_file_summary()` 出文件清单都依赖本地文件
